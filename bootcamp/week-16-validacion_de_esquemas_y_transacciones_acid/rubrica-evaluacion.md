# Rúbrica de Evaluación — Semana 16

## Conocimiento 🧠 (30%)

| Criterio | Puntaje |
|---|---|
| Explica qué es `$jsonSchema` y para qué sirve `validator` | 10 pts |
| Describe la diferencia entre `validationLevel: "strict"` y `"moderate"` | 10 pts |
| Explica las 4 propiedades ACID y cómo MongoDB las garantiza desde 4.0 | 10 pts |

## Desempeño 💪 (40%)

| Criterio | Puntaje |
|---|---|
| Crea una colección con `validator` usando tipos y `required` correctamente | 10 pts |
| Modifica el esquema de una colección existente con `collMod` | 10 pts |
| Inicia, ejecuta operaciones y hace `commitTransaction` correctamente | 10 pts |
| Implementa `abortTransaction` en el bloque catch | 10 pts |

## Producto 📦 (30%)

| Criterio | Puntaje |
|---|---|
| `$jsonSchema` aplicado a la colección principal del dominio asignado | 15 pts |
| Transacción multi-documento funcional con `insertOne` + `updateOne` | 15 pts |

**Puntaje mínimo de aprobación**: 70 puntos sobre 100
