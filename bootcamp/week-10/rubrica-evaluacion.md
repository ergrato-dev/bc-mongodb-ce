# Rúbrica de Evaluación — Semana 10

## Aggregation Pipeline II: Acumuladores Avanzados

### Conocimiento 🧠 (30%)

| Criterio | Puntaje |
|---|---|
| Explica la diferencia entre `$first`/`$last` y `$push`/`$addToSet` | 15 |
| Describe cuándo usar `$addFields` en lugar de `$project` | 15 |

### Desempeño 💪 (40%)

| Criterio | Puntaje |
|---|---|
| `$group` con `$first`, `$last` y `$push` funcionando | 10 |
| `$addToSet` para coleccionar valores únicos en el grupo | 10 |
| `$addFields` con campo calculado en el pipeline | 10 |
| `$cond` o `$ifNull` dentro de `$project` o `$addFields` | 10 |

### Producto 📦 (30%)

| Criterio | Puntaje |
|---|---|
| Pipeline con mínimo 4 etapas distintas | 10 |
| Uso de al menos 3 acumuladores diferentes | 10 |
| Comentarios explicativos en cada etapa | 10 |

Puntaje mínimo de aprobación: **70 puntos**.
