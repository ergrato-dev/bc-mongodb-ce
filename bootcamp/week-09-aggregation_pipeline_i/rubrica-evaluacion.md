# Rúbrica de Evaluación — Semana 09

## Aggregation Pipeline I

### Conocimiento 🧠 (30%)

| Criterio | Puntaje |
|---|---|
| Explica qué es un pipeline y cómo fluyen los documentos entre etapas | 15 |
| Diferencia cuándo usar `aggregate()` vs `find()` | 15 |

### Desempeño 💪 (40%)

| Criterio | Puntaje |
|---|---|
| Pipeline con `$match` + `$project` funcionando correctamente | 10 |
| Pipeline con `$sort` + `$limit` + `$skip` | 10 |
| Pipeline con `$group` y al menos `$sum` o `$count` | 10 |
| Uso de `$match` antes de `$group` para optimizar el pipeline | 10 |

### Producto 📦 (30%)

| Criterio | Puntaje |
|---|---|
| Proyecto con colección del dominio asignado (mínimo 15 docs) | 10 |
| Mínimo 3 pipelines distintos implementados | 10 |
| Comentarios explicativos en cada etapa del pipeline | 10 |

## Niveles de Desempeño

| Nivel | Rango |
|---|---|
| Excelente | 90–100 |
| Satisfactorio | 70–89 |
| En desarrollo | 50–69 |
| Insuficiente | 0–49 |

Puntaje mínimo de aprobación: **70 puntos**.
